---
title: FlexBox
description: "C'est ainsi qu'on définit un container flex, il est block par défaut ou inline selon la valeur donnée. Cela crée un contexte flex pour tous les descendants directs."
sidebar:
  position: 0
---

C'est ainsi qu'on définit un container flex : il est **block** par défaut ou **inline** selon la valeur donnée.  
Cela crée un **contexte flex** pour tous les descendants directs.

---

## Display

| Propriété | Valeur        | Description                                                                 |
|-----------|---------------|-----------------------------------------------------------------------------|
| `display` | `flex`        | Génère un container flex de niveau **block** à l'intérieur de l'élément.   |
|           | `inline-flex` | Génère un container flex de niveau **inline** à l'intérieur de l'élément.  |

---

## Flex-Direction

La propriété `flex-direction` établit l’axe principal du container.

| Propriété        | Valeur           | Description                                                                 |
|------------------|-----------------|-----------------------------------------------------------------------------|
| `flex-direction` | `row`           | (par défaut) : de gauche à droite (ou droite à gauche si langue RTL).       |
|                  | `row-reverse`   | Inverse le sens de `row`.                                                   |
|                  | `column`        | De haut en bas.                                                             |
|                  | `column-reverse`| Comme `column`, mais du bas vers le haut.                                   |

🔗 [Exemple interactif MDN](https://interactive-examples.mdn.mozilla.net/pages/css/flex-direction.html)

---

## Flex-Wrap

Définit si le container comprend une seule ligne ou plusieurs, et la direction de l’empilement.

| Propriété    | Valeur         | Description                                                                 |
|--------------|---------------|-----------------------------------------------------------------------------|
| `flex-wrap`  | `nowrap`      | (par défaut) Une seule ligne, pouvant déborder du contenant.                 |
|              | `wrap`        | Multi-lignes, retour automatique à la ligne.                                |
|              | `wrap-reverse`| Multi-lignes, empilement inversé (de droite à gauche ou bas vers haut).     |

🔗 [Exemple interactif MDN](https://interactive-examples.mdn.mozilla.net/pages/css/flex-wrap.html)

---

## Flex-Flow

Raccourci combinant `flex-direction` et `flex-wrap`.

| Propriété   | Valeur            | Description                                            |
|-------------|------------------|--------------------------------------------------------|
| `flex-flow` | `flex-direction` | Définit l’axe principal.                               |
|             | `flex-wrap`      | Définit le retour à la ligne (axe secondaire).         |

Par défaut : `row nowrap`.

🔗 [Exemple interactif MDN](https://interactive-examples.mdn.mozilla.net/pages/css/flex-flow.html)

---

## Justify-Content

Définit l’alignement **le long de l’axe principal**.

| Valeur          | Description                                                                 |
|-----------------|-----------------------------------------------------------------------------|
| `flex-start`    | (par défaut) Items regroupés au début de la ligne.                          |
| `flex-end`      | Items regroupés en fin de ligne.                                            |
| `center`        | Items centrés sur la ligne.                                                 |
| `space-between` | Répartition sur toute la ligne (1er au start, dernier à l’end).             |
| `space-around`  | Espacement égal autour de chaque item.                                      |

🔗 [Exemple interactif MDN](https://interactive-examples.mdn.mozilla.net/pages/css/justify-content.html)

---

## Align-Items

Définit l’alignement **le long de l’axe secondaire (cross-axis)**.

| Valeur       | Description                                                                 |
|--------------|-----------------------------------------------------------------------------|
| `flex-start` | Aligné au début de l’axe secondaire.                                        |
| `flex-end`   | Aligné à la fin de l’axe secondaire.                                        |
| `center`     | Centré sur l’axe secondaire.                                                |
| `baseline`   | Alignement sur la ligne de base du texte.                                   |
| `stretch`    | (par défaut) Étire les items pour remplir le container.                     |

🔗 [Exemple interactif MDN](https://interactive-examples.mdn.mozilla.net/pages/css/align-items.html)

---

## Align-Content

Aligne les **lignes** d’un container flex lorsqu’il y a plusieurs lignes.  
⚠️ Sans effet si le container n’a qu’une seule ligne.

| Valeur          | Description                                                                 |
|-----------------|-----------------------------------------------------------------------------|
| `flex-start`    | Lignes regroupées en haut du container.                                     |
| `flex-end`      | Lignes regroupées en bas.                                                   |
| `center`        | Lignes regroupées au centre.                                                |
| `space-between` | Répartition avec extrêmes collés au début et à la fin.                      |
| `space-around`  | Espacement égal autour de chaque ligne.                                     |
| `stretch`       | (par défaut) Étire les lignes pour remplir l’espace.                        |

---

## Gap

La propriété `gap` définit les **espaces entre les items**, en remplaçant souvent l’usage de `margin`.

🔗 [Exemple interactif MDN](https://interactive-examples.mdn.mozilla.net/pages/css/gap.html)

---

## Order

Contrôle l’ordre d’affichage des items flex indépendamment du DOM.

| Propriété | Valeur             | Description                                   |
|-----------|--------------------|-----------------------------------------------|
| `order`   | `nombre entier`    | Positif ou négatif (ex. `-1`, `0`, `2`, etc.) |

---

## Flex-Grow

Définit la **capacité d’un item à grandir**.

| Propriété     | Valeur            | Description                                             |
|---------------|------------------|---------------------------------------------------------|
| `flex-grow`   | `nombre entier`  | (par défaut = 0). Valeurs négatives interdites.         |

---

## Flex-Shrink

Définit la **capacité d’un item à rétrécir**.

| Propriété      | Valeur           | Description                                             |
|----------------|-----------------|---------------------------------------------------------|
| `flex-shrink`  | `nombre entier` | (par défaut = 1). Valeurs négatives interdites.         |

---

## Flex-Basis

Définit la **taille initiale** d’un élément avant distribution de l’espace.

| Propriété     | Valeur          | Description                   |
|---------------|----------------|-------------------------------|
| `flex-basis`  | `longueur`     | Taille par défaut.            |
|               | `auto` (defaut)| Calculée en fonction du contenu.|

---

## Align-Self

Permet à un **item individuel** d’écraser la valeur de `align-items`.

| Valeur       | Description                                                             |
|--------------|-------------------------------------------------------------------------|
| `auto`       | Valeur par défaut.                                                      |
| `flex-start` | Aligné au début du container.                                           |
| `flex-end`   | Aligné à la fin du container.                                           |
| `center`     | Centré sur l’axe secondaire.                                            |
| `baseline`   | Alignement sur la ligne de base du texte.                               |
| `stretch`    | Étire l’item pour remplir le container.                                 |

---

## À retenir

- `display: flex` → crée un conteneur flexible.  
- `flex-direction` → définit l’axe principal (`row`/`column`).  
- `flex-wrap` → gère le retour à la ligne.  
- `justify-content` → aligne sur l’axe principal.  
- `align-items` → aligne sur l’axe secondaire.  
- `align-self` → surchage l’alignement d’un item.  
- `flex-grow`, `flex-shrink`, `flex-basis` → contrôlent la répartition de l’espace.  
- `gap` → préférable à `margin` pour gérer les espacements.  
- `order` → change l’ordre visuel sans toucher au DOM.  
