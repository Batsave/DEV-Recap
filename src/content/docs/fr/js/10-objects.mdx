---
title: Objets
description: "Introduction aux objets en JavaScript : création, accès aux propriétés et parcours."
sidebar:
  position: 9
---

## Créer un objet

```js
let personne = {
  nom: "Alice",
  age: 25,
  actif: true
};

console.log(personne.nom);  // Alice
console.log(personne["age"]); // 25
```

---

## Modifier un objet

```js
let voiture = { marque: "Tesla", modele: "Model 3" };
voiture.couleur = "rouge";

console.log(voiture);
```

---

## Parcourir un objet

```js
let utilisateur = { nom: "Bob", age: 30 };

for (let cle in utilisateur) {
  console.log(cle, ":", utilisateur[cle]);
}
```

---

## JSON

Le **JSON** (JavaScript Object Notation) est un format de données basé sur la syntaxe des objets JS.

```js
let json = '{"nom": "Alice", "age": 25}';
let objet = JSON.parse(json);
console.log(objet.nom); // Alice

let chaine = JSON.stringify(objet);
console.log(chaine); // {"nom":"Alice","age":25}
```
