---
title: Classes
description: "Introduction aux classes en JavaScript et à l’héritage."
sidebar:
  position: 15
---

## Déclaration d'une classe

```js
class Personne {
  constructor(nom, age) {
    this.nom = nom;
    this.age = age;
  }

  sePresenter() {
    console.log(`Je m'appelle ${this.nom} et j'ai ${this.age} ans.`);
  }
}

let alice = new Personne("Alice", 25);
alice.sePresenter(); // Je m'appelle Alice et j'ai 25 ans.
```

---

## Héritage

```js
class Animal {
  constructor(nom) {
    this.nom = nom;
  }

  parler() {
    console.log(`${this.nom} fait un bruit.`);
  }
}

class Chien extends Animal {
  parler() {
    console.log(`${this.nom} aboie.`);
  }
}

let rex = new Chien("Rex");
rex.parler(); // Rex aboie.
```
